spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    hikari:
      username: sa
      password: sa
  jpa:
    hibernate:
      ddl-auto: create